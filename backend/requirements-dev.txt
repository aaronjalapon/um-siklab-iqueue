# =============================================================================
# IQueue — Development Dependencies
# Pinned to latest stable versions as of May 2026
# =============================================================================
# Install with: pip install -r requirements-dev.txt
# These extend requirements.txt for local development.

-r requirements.txt

# Linting & Formatting
ruff>=0.15.0
black>=26.0.0

# Testing
pytest>=9.0.0
pytest-asyncio>=1.4.0
pytest-cov>=7.0.0
httpx>=0.28.0

# Test database (in-memory SQLite for fast unit tests)
aiosqlite>=0.22.0

# Load Testing
locust>=2.27.0

# Type Checking
mypy>=1.10.0
types-python-dateutil>=2.9.0
